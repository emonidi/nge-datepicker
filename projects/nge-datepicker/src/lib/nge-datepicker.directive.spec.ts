import { NgeDatepickerDirective } from './nge-datepicker.directive';

describe('NgDatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgeDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
